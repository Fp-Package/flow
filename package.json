{
  "name": "flowjs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild ts/index.ts --bundle --format=esm --outfile=/index.js --watch",
    "serve": "browser-sync start --server ../ --files '../**/*'",
    "start": "npm-run-all --parallel build serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^3.0.3",
    "esbuild": "^0.24.0",
    "nodemon": "^3.1.7",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.6.3"
  }
}
