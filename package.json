{
  "name": "flowjs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild ts/index.ts --bundle --format=esm --outfile=/index.js --watch",
    "build:scss": "sass style.scss style.css",
    "watch:scss": "sass --watch style.scss style.css",
    "serve": "browser-sync start --server ../ --files '../**/*' --poll",
    "start": "npm-run-all --parallel build watch:scss serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^3.0.3",
    "esbuild": "^0.24.0",
    "nodemon": "^3.1.7",
    "npm-run-all": "^4.1.5",
    "sass": "^1.81.0",
    "typescript": "^5.6.3"
  }
}
